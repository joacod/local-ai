# Runtime tuning compatibility link

This filename is retained because the untouched `mlx/` and `llama-cpp/`
guides still link to it. The canonical document is the
[Runtime Configuration Tuning guide](./runtime-tuning.md).

This repository does not maintain a cross-runtime comparison plan or model
benchmark leaderboard.